[user]
    name = Dominik Rehák
    email = drehak@redhat.com
[color]
    ui = auto
[diff]
    algorithm = minimal
[alias]
    aliases = config --get-regexp alias.*

    bra = branch -avv
    brv = branch -vv
    co = checkout
    cmm = commit -m
    cm = commit --verbose
    cma = commit --verbose --amend
    dc = diff --cached
    fall = fetch --all
    lg = log --graph
    lga = log --graph --all
    lgo = log --graph --oneline
    lgao = log --graph --all --oneline
    pl = pull
    ps = push
    ph = push
    rb = rebase
    rbc = rebase --continue
    rbi = rebase --interactive
    sh = show
    shw = show --word-diff=color
    shc = show --word-diff=color --word-diff-regex=.

    patch = commit --amend --no-edit
    mbfp = merge-base --fork-point  # the fuck was this
[pull]
    rebase = false
[log]
    graphColors = bold red,bold green,bold yellow,bold blue,bold magenta,bold cyan,bold white
