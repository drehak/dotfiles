[user]
    name = Dominik Rehák
    email = drehak@redhat.com
[color]
    ui = auto
[diff]
    algorithm = minimal
[alias]
    aliases = config --get-regexp alias.*

    br = branch
    bra = branch --all vv
    brv = branch -vv
    co = checkout
    cmm = commit --message
    cm = commit --verbose
    cma = commit --verbose --amend
    cmam = commit --all --message
    cmf = commit --message '' --fixup
    cp = cherry-pick
    cpa = cherry-pick --abort
    cpc = cherry-pick --continue
    cps = cherry-pick --skip
    dc = diff --cached
    ds = diff --stat
    dcs = diff --cached --stat
    dw = diff --word-diff=color
    dwc = diff --word-diff=color --word-diff-regex=.
    fall = fetch --all
    lg = log --graph
    lga = log --graph --all
    lgo = log --graph --oneline
    lgao = log --graph --all --oneline
    lgs = log --graph --stat
    lgas = log --graph --all --stat
    lgos = log --graph --oneline --stat
    lgaos = log --graph --all --oneline --stat
    pl = pull
    ps = push
    ph = push
    rb = rebase
    rba = rebase --abort
    rbc = rebase --continue
    rbe = rebase --edit-todo
    rbi = rebase --interactive
    rbias = rebase --interactive --autosquash
    rbs = rebase --skip
    rest = restore --staged
    sh = show
    shw = show --word-diff=color
    shwc = show --word-diff=color --word-diff-regex=.
    shs = show --stat
    st = stash
    sta = stash apply
    stl = stash list
    stls = stash list --stat
    stp = stash pop
    sts = stash show
    stss = stash show --stat

    patch = commit --amend --no-edit
    apatch = commit --all --amend --no-edit

    mbfp = merge-base --fork-point  # show last common ancestor of two branches
[pull]
    rebase = true
[log]
    graphColors = bold red,bold green,bold yellow,bold blue,bold magenta,bold cyan,bold white
[credential]
    helper = store
[stash]
    showStat = true
    showPatch = true
[init]
	defaultBranch = master
